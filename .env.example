# ---------------------------------------------------------------------
# Copie este arquivo para .env (ou .env.local) e ajuste o valor.
# Atenção: variáveis VITE_ ficam visíveis no código publicado.
# Nunca coloque senhas ou segredos aqui.
# ---------------------------------------------------------------------

# Endereço do backend, SEM barra no final.
# Desenvolvimento: http://localhost:3000
# Produção: a URL do serviço no Render/Railway (ex: https://sge-api.onrender.com)
VITE_API_URL=http://localhost:3000
